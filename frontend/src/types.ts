export interface UserInfo {
  username: string;
  color: string;
  userId: string;
}

export interface DocumentMetadata {
  docId: string;
  title: string;
  lastModified: number;
  createdAt: number;
}

